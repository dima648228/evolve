ОСНОВНОЙ СИНТАКСИС

ОПЕРАТОРЫ:
    МАТЕМАТИЧЕСКИЕ:
        %
    ЛОГИЧЕСКИЕ:
        &&, ||
    ПРИСВАИВАНИЕ:
        *=, /=, +=, -=, %=
    БИТНЫЕ:
        &, <<, >>, |, ^
ТИПЫ ДАННЫХ:
    МАССИВЫ,
    СПИСКИ
ФУНКЦИОНАЛЬНОЕ ПРОГРАММИРОВАНИЕ:
    ФУНКЦИИ,
    ПРОЦЕДУРЫ,
    ЛОКАЛЬНОСТЬ И ГЛОБАЛЬНОСТЬ ПЕРЕМЕННЫХ
УПРАВЛЕНИЕ ПОТОКОМ:
    ПРЕРЫВАНИЯ:
        break,
        continue
    ЦИКЛЫ:
        while, 
        for
    ВОЗВРАТ:
        return
ООП:
    СТРУКТУРЫ,
    КЛАССЫ, ОБЬЕКТЫ